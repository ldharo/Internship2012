This repertory contains all files related to RCFAE.

The file envFailure.py is a little program that serves to demonstrate why we needed a new implementation of maps.

The repertory compareTrace contains all files needed to compare the traces produced by an iterative interpreter when we played with promote and elidable annotations.

The benchmarks repertory contains every file related to the benchmarks section of the report.
